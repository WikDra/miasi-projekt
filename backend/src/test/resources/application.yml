spring:
  datasource:
    url: jdbc:h2:mem:school-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    username: sa
    password: ""
    driver-class-name: org.h2.Driver

app:
  jwt:
    secret: test-jwt-secret-that-is-long-enough-for-hmac
    expiration-seconds: 3600
