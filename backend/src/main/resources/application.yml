spring:
  application:
    name: school-backend
  datasource:
    url: jdbc:h2:file:./data/school-db;MODE=PostgreSQL;DB_CLOSE_ON_EXIT=FALSE;DATABASE_TO_UPPER=false
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true

server:
  port: 8080